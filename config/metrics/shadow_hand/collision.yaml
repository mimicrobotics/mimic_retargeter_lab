_target_: mimic_retargeter_lab.metrics.CollisionMetric
config:
  display_name: "Collision"
  # Penetrations <= this threshold (mm) are ignored to account for soft-body compliance
  penetration_tolerance_mm: 2.0

data_source_cfg: ${dataset.data_source_cfg}
